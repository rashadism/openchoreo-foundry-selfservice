# One-time platform-engineer setup.
#
# Compatibility fallback for provider CRs that omit spec.forProvider.projectEndpoint.
# The ResourceTypes normally render the endpoint from the binding's
# resourceTypeEnvironmentConfigs directly into each managed CR (see
# app/openchoreo/deploy.yaml).
#
# Change the values below for your environment.
apiVersion: v1
kind: ConfigMap
metadata:
  name: foundry-account
  namespace: provider-foundry
data:
  projectEndpoint: https://rashad20-9496-resource.services.ai.azure.com/api/projects/rashad20-9496
